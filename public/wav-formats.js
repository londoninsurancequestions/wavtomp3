/** @deprecated Use conversion-formats.js — kept for backward compatibility */
export {
  WAV_ROUTES as WAV_FORMATS,
  getRoute as getFormat,
  relatedFormatsHtml,
  formatCtasHtml,
} from './conversion-formats.js';
